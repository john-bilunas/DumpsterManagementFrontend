import React,{useState} from 'react'

const AddItem = ({allItemCategoriesList, onAdd}) => {

        const [categoryId, setCategoryId] = useState(0);
        const [name, setName] = useState(0);
        const [price, setPrice] = useState(0);

    return (
        <>
        
        </>
        
      )
}

export default AddItem;